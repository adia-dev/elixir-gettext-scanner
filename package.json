{
  "name": "gettext-scanner",
  "displayName": "gettext-scanner",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:elixir",
    "onLanguage:phoenix-heex",
    "onCommand:gettext-scanner.scan"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gettext-scanner.scan",
        "title": "Scan the project for gettext strings"
      }
    ],
    "configuration": {
      "title": "Gettext Scanner",
      "properties": {
        "gettext-scanner.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Gettext Scanner"
        },
        "gettext-scanner.scanPath": {
          "type": "string",
          "default": "lib",
          "description": "Scan Path"
        },
        "gettext-scanner.scanPattern": {
          "type": [
            "string",
            "array"
          ],
          "default": "**/*.js",
          "description": "Scan Pattern"
        },
        "gettext-scanner.scanExcludePattern": {
          "type": "string",
          "default": "**/node_modules/**",
          "description": "Scan Exclude Pattern"
        },
        "gettext-scanner.scanOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Scan On Save"
        },
        "gettext-scanner.scanOnStartup": {
          "type": "boolean",
          "default": true,
          "description": "Scan On Startup"
        }
      }
    },
    "languages": [
      {
        "id": "elixir",
        "aliases": [
          "Elixir",
          "elixir"
        ],
        "extensions": [
          ".ex",
          ".exs",
          ".eex",
          ".leex",
          ".heex"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gettext-scanner",
          "title": "Gettext Scanner",
          "icon": "resources/images/icon.png"
        }
      ]
    },
    "views": {
      "gettext-scanner": [
        {
          "id": "gettext-scanner",
          "name": "Gettext Scanner"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "gettext-scanner.gettext-scanner",
        "contents": "Scan the project for gettext strings",
        "when": "config.gettext-scanner.enabled && view == gettext-scanner.gettext-scanner && viewItem == gettext-scanner.gettext-scanner"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.9.3",
    "@vscode/test-electron": "^2.2.0"
  }
}
